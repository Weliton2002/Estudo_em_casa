#include <stdio.h>

/*Faça um programa que declare uma variável inteira e mostre seu conteúdo para o
usuário. Execute o programa algumas vezes. Qual o significado do valor que está sendo
impresso pelo programa?*/

int main() {
    // Declaração da variável inteira
    int x;

    // Atribuição de um valor à variável
    x = 42;

    // Exibição do conteúdo da variável para o usuário
    printf("O conteudo da variavel e: %d\n", x);

    return 0;
}